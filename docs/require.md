1. 每次上传完文件刷新网页， 刷新网页请求个人信息 （存储空间会变化） 查看个人中心也需要请求个人信息

2. 操作一栏按你太小了 都看不见了，然后操作这个标题可以不写 然后现在大小和类型列挨得太紧了
3. 增加在操作栏添加一个功能 复制文件或文件夹， 如果操作栏ui过长 可以用。。。折叠一下， 复制和移动逻辑类似 访问的后端api如下
post请求 user-file/copy 参数放在body里 参数有 long originalId 表示移动的文件或文件夹的id ，long targetId 表示 复制到的文件夹id，其中根目录id为0
